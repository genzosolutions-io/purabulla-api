interface ImageData {
    type: string;
    size?: string;
    url: string;
}
export declare class ImageSearch {
    static findImages(url: string, showAllImages?: boolean): Promise<ImageData | ImageData[]>;
}
export {};
